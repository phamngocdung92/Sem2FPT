import { StickyInforDirective } from './sticky-infor.directive';

describe('StickyInforDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyInforDirective();
    expect(directive).toBeTruthy();
  });
});
